# C++11 零基础知识图谱：总图与选题索引

## 1. 学习前提

这套图谱不假定学习者学过 C，也不假定学过 C++98/03。

- 从“程序、源文件、编译器、对象、类型、值、表达式、语句”开始解释。
- 主线严格采用 ISO C++11。
- C 语言内容只在理解 C++ 接口确实需要时说明，不单独按 C11 教学。
- 编译器、链接器、静态库、动态库、CMake 和 Linux/POSIX 会单独标为工程知识，不能冒充 C++11 语法或标准库。
- 后续题库只使用已经讲过或已被选入范围的知识点，不假定隐藏前置知识。
- 这里的“完整知识图谱”覆盖可独立学习、解释和出题的概念节点；它不是受版权保护的 ISO 标准逐条复制，也不逐项抄录每个标准库函数的全部重载。

## 2. 标准版本边界

下列常见内容不属于 C++11，不能混入主线：

| 内容 | 首次所属版本 |
|---|---|
| 二进制字面量、数字分隔符 | C++14 |
| `std::make_unique` | C++14 |
| 泛型 Lambda `[](auto x)` | C++14 |
| Lambda 初始化捕获 `[x = expr]` | C++14 |
| `decltype(auto)`、变量模板 | C++14 |
| 结构化绑定、`if constexpr`、折叠表达式 | C++17 |
| `std::optional`、`variant`、`any`、`string_view` | C++17 |
| `std::filesystem`、`std::scoped_lock` | C++17 |
| Concepts、Ranges、Modules、`std::jthread` | C++20 |

它们可以作为“版本辨析”出现，但不会被当作 C++11 可用功能。

## 3. 总依赖图

```text
Z0 计算机、程序、源文件与工具
  ↓
Z1 最小程序、记号、名字、语句与基础 I/O
  ↓
Z2 类型、对象、值、变量、初始化、const
  ↓
Z3 表达式、运算符、转换、求值顺序与错误行为
  ↓
Z4 分支、循环、函数、作用域、声明与定义
  ├───────────────┬──────────────────┐
  ↓               ↓                  ↓
Z5 数组/string/   Z6 指针/引用/      Z7 分离编译/
   vector            生命周期           链接/库
  └───────────────┬──────────────────┘
                  ↓
Z8 类、对象、构造、析构、封装
  ↓
Z9 复制控制、RAII、右值引用、移动语义、智能指针
  ├────────────────────┐
  ↓                    ↓
Z10 继承、多态、RTTI   Z11 模板、类型萃取、完美转发
  └──────────┬─────────┘
             ↓
Z12 Lambda、可调用对象、STL 容器/迭代器/算法
  ├──────────────┬─────────────────┐
  ↓              ↓                 ↓
Z13 异常安全     Z14 文件与流       Z15 线程、锁、异步任务
                                     ↓
                              Z16 原子与内存模型
                                     ↓
                         Z17 Linux/CMake/网络工程扩展
```

错误分类、未定义行为、生命周期、资源所有权不是末尾才学的孤立章节，而是贯穿所有模块。

## 4. 完整模块目录

### A. 零基础与核心语言

详解文件：[01_基础核心.md](sections/01_基础核心.md)

| 范围 | 内容 | 建议层级 |
|---|---|---|
| `B0` | 程序、源代码、可执行文件和最基本术语 | 起点 |
| `B1` | 预处理、编译、汇编、链接、翻译单元、ODR | 基础 |
| `B2` | 记号、标识符、命名空间、字面量、预处理器 | 基础 |
| `B3` | 布尔、字符、整数、浮点、`void`、枚举和数据表示 | 基础 |
| `B4` | 声明、定义、初始化、赋值、`auto`、`decltype` | 基础 |
| `B5` | `const`、顶层/底层 const、`constexpr` | 基础 |
| `B6` | 表达式、值类别、运算符、求值顺序 | 核心 |
| `B7` | 整数提升、算术转换、显式转换 | 核心 |
| `B8` | `if`、`switch`、循环和跳转语句 | 基础 |
| `B9` | 函数、传参、返回、重载、默认实参、递归 | 核心 |
| `B10` | `cin/cout`、`getline`、流状态和格式控制 | 基础 |
| `B11` | 内建数组、多维数组和 `std::array` | 核心 |
| `B12` | 字符数组、C 字符串和 `std::string` | 核心 |
| `B13` | `std::vector`、容量、访问、修改和失效 | 核心 |
| `B14` | 指针、引用、动态对象和所有权入门 | 核心 |
| `B15` | 作用域、链接、存储期、生命周期和临时对象 | 核心 |
| `B16` | 编译/链接/运行/逻辑错误及行为分类 | 全程贯穿 |

### B. 类、资源、模板、Lambda 与 STL

详解文件：[02_类模板STL.md](sections/02_类模板STL.md)

| 范围 | 内容 | 建议层级 |
|---|---|---|
| `O1–O2` | 类、对象、成员、封装、const 成员、友元和组合 | 类入门 |
| `O3–O4` | 构造、成员初始化、委托构造、析构与临时对象 | 类核心 |
| `O5–O7` | 特殊成员、复制控制、静态成员和运算符重载 | 类核心 |
| `O8–O9` | 继承、多态、RTTI、对象布局与 ABI 边界 | 进阶 |
| `R1–R3` | 存储、资源、原始动态内存和 RAII | 资源基础 |
| `R4–R5` | 值类别、右值引用、`std::move` 和移动语义 | C++11 核心 |
| `R6–R8` | `unique_ptr`、`shared_ptr`、`weak_ptr` 和所有权设计 | C++11 核心 |
| `R9` | placement new、手工生存期和底层内存 | 高级 |
| `T1–T3` | 函数模板、类模板、实参推导和别名模板 | 模板基础 |
| `T4–T5` | 特化、实例化、依赖名称和两阶段查找 | 模板进阶 |
| `T6–T8` | 可变参数模板、转发引用、类型萃取和 SFINAE | C++11 进阶 |
| `L1–L2` | Lambda 语法和捕获 | C++11 核心 |
| `L3–L5` | 闭包、`std::function`、`bind`、生命周期和算法 | 核心 |
| `S1–S3` | STL 模型、半开区间、迭代器和通用工具 | STL 基础 |
| `S4–S5` | 顺序容器和 `vector` 深入 | STL 核心 |
| `S6–S8` | 有序/无序关联容器和容器适配器 | STL 核心 |
| `S9–S12` | 迭代器失效、算法、比较器、移动与 emplace | STL 核心 |
| `S13` | 分配器与 `allocator_traits` | 高级 |
| `X1–X40` | 40 组必须区分的易混概念 | 复习索引 |

### C. 异常、并发、I/O 与工程

详解文件：[03_系统并发工程.md](sections/03_系统并发工程.md)

| 范围 | 内容 | 性质 |
|---|---|---|
| `EX-01–EX-18` | 异常、栈展开、`noexcept`、异常安全和跨边界错误 | C++11 |
| `MEM-01–MEM-28` | 对象生存期、智能指针、对齐、对象表示和诊断 | C++11 + 工程 |
| `TH-01–TH-23` | `thread`、锁、条件变量、future、promise、async | C++11 |
| `AT-01–AT-21` | 数据竞争、happens-before、原子和内存序 | C++11 |
| `IO-01–IO-23` | 流、文件、格式化、二进制 I/O 和序列化边界 | C++11 + 工程 |
| `BUILD-01–BUILD-20` | 编译、链接、ODR、ABI、静态库和动态库 | 工具链 |
| `CMAKE-01–CMAKE-16` | Target、依赖、构建配置、测试和安装 | 非 C++11 |
| `LINUX-01–LINUX-19` | ELF、`.a/.so`、GDB、文件描述符和 POSIX | 非 C++11 |

### D. C++11 完整性补充层

详解文件：

- [04_完整性补充.md](sections/04_完整性补充.md)
- [05_语法并发与算法补充.md](sections/05_语法并发与算法补充.md)

| 范围 | 内容 | 建议层级 |
|---|---|---|
| `U1–U3` | 用户定义字面量、属性、尾置返回、引用限定、对齐和联合体 | 进阶 |
| `LIB1–LIB2` | 通用工具、RTTI、类型萃取和低层内存工具 | 核心/进阶 |
| `LIB3–LIB8` | chrono、ratio、random、regex、bitset、数值、locale 和诊断 | 专项 |
| `GX1–GX6` | 名字查找、预处理、初始化、范围 for、变参和终止路径 | 基础补全 |
| `ITER1、ALG1–ALG8` | 迭代器工具和标准算法完整分类 | STL 核心 |
| `CX1–CX6` | 线程细节、future、原子初始化、内存序和并发边界 | 并发进阶 |
| `MX1–MX5` | 数学、字符、C 兼容接口和安全解析 | 专项 |

## 5. 从零学习的建议阶段

### 阶段 0：第一次写程序

范围：`B0–B4、B8、B10`

目标：能读懂最小程序，理解变量、类型、初始化、基本输入输出和控制流。这里不能提前假定已经理解指针、类或 STL。

### 阶段 1：语言基础稳固

范围：`B5–B9、B11–B16`

目标：理解表达式、类型转换、函数、数组、字符串、vector、指针、引用、作用域和生命周期。

### 阶段 2：面向对象与资源管理

范围：`O1–O7、R1–R8、EX-01–EX-14`

目标：正确设计对象，掌握构造析构、复制移动、RAII、智能指针和异常安全。

### 阶段 3：泛型编程与 STL

范围：`T1–T8、L1–L5、S1–S13`

目标：理解模板实例化、完美转发、Lambda、容器、迭代器、算法契约和失效规则。

### 阶段 4：多态与系统能力

范围：`O8–O9、IO-01–IO-23、TH-01–TH-23`

目标：掌握运行时多态、文件 I/O 和基于锁的并发。

### 阶段 5：高并发与 Linux 工程

范围：`AT-01–AT-21、CX1–CX6、BUILD-01–BUILD-20、CMAKE-01–CMAKE-16、LINUX-01–LINUX-19`

目标：理解 C++11 内存模型，并把语言能力连接到 Linux 编译、链接、动态库和高并发工程。

## 6. 所有题目批次的生成约束

当前主线覆盖状态：

| 主模块 | 叶子节点 | 对应题目 |
|---|---:|---:|
| `01_基础核心.md` | 99/99 | 001–240 中完成全覆盖并包含巩固题 |
| `02_类模板STL.md` | 266/266 | 241–510 |
| `03_系统并发工程.md` | 168/168 | 511–690 |
| **合计** | **533/533** | **690 题，缺失节点 0** |

题目按每批 30 道切分为 `questions/batch_01_001_030.md` 至
`questions/batch_23_661_690.md`。每题下面直接给出答案；同一节点的基础题和综合题尽量相邻。

每一批题目固定遵守：

1. 每批恰好 30 题；编号承接上一批，连续且不重复。
2. 每题下面直接给答案和必要解释。
3. 不假定学过未被选中的知识点。
4. 同一知识点的题放在一起。
5. 每题标注对应图谱节点，例如 `【B4.3】`。
6. 题型以回答基础知识点、判断、代码阅读和最小纠错为主。
7. 不用 C11 规则替代 C++11 规则。
8. 不混入 C++14/17；版本辨析题必须明确标注。
9. 输出 Markdown，不生成 Word。
10. 用户提出的技术疑问继续写入 C++11 错题本。

## 7. 如何选择后续范围

可以直接回复编号，例如：

```text
下一批范围：B0–B4
```

也可以精确到子节点：

```text
下一批范围：B3.1–B3.7、B4.1–B4.5
```

如果需要跨模块：

```text
下一批范围：B14、O1–O3、R1–R3
```

完整性补充层也可以单独选择：

```text
下一批范围：GX3–GX5、ITER1、ALG1–ALG4
```

若没有指定范围，则优先选择尚未覆盖或需要强化的节点，但仍不得把未讲知识当作默认前置知识。
