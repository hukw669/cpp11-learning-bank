# 项目 Agent 协作规则

本文件适用于整个仓库。题库内容仍须遵守 [`docs/题库生成与维护规则.md`](docs/题库生成与维护规则.md)，错题本内容仍须遵守 [`docs/错题本维护规则.md`](docs/错题本维护规则.md)；本文件补充多 agent 调度、共享工作区和 Git 发布规则。发生冲突时，以用户本轮明确要求为最高优先级。

## 1. 主 Agent 与错题本 Agent 的职责

- 用户提出技术问题时，主 agent 必须立即给出简洁、直接的答案，不等待错题本落盘、验证、提交或推送。
- 同时需要记录该问题时，主 agent 将错题本维护异步委派给专用 subagent；落盘工作不得阻塞快问快答。
- 主 agent 对当场技术答案的正确性负责；错题本 agent 对去重、持久化内容、格式、验证和发布质量负责。委派不转移主 agent 的答案责任。
- 错题本 agent 完成后向主 agent 报告，主 agent 只需在后续合适时机告知用户持久化结果，无须让用户等待。

## 2. 技术提问的记录与编号

用户主动提出技术问题时，默认触发错题本维护流程，除非用户明确要求不要记录。

1. 先搜索 `wrongbook/wrongbook.md` 的标题、问题、核心答案和相关术语。
2. 同一知识点的追问、补充或纠错更新原 `WNNN`，不得创建近义重复条目。
3. 只有考查核心确实不同，才创建新条目。
4. 新编号使用仓库历史中**从未使用过的最大编号加一**。不得填补删除留下的空号，也不得复用已经删除的编号；有疑问时同时检查当前文件和该文件的 Git 历史。
5. 新增或改名后同步维护目录、稳定锚点和 `[返回目录](#toc)`；删除条目时不重排其他历史编号。

这里的“最大历史编号加一”是对错题本规则中“连续编号”的明确化：连续增长指编号时间线，不表示可以回填空洞。

## 3. 单 Writer 与任务排队

- 同一个目标文件同一时刻只能有一个 writer。`wrongbook/wrongbook.md` 尤其禁止多个 agent 并行写入。
- 主 agent 派发前先查看活动 agent；若已有 wrongbook writer，新问题加入现有 writer 的待办，或等待其完成后再启动下一 writer，不得再创建一个并行 writer。
- 后续问题可以先由主 agent 立即回答；文件写入在后台串行处理。
- writer 获得文件所有权后，其他 agent 只能只读检查该文件。所有权持续到修改已验证、提交并明确报告完成，或报告失败并释放所有权。
- 任一 agent 在 `apply_patch`、暂存或提交前，都必须重新检查活动 agent 与 `git status`，确认没有同文件 writer 或并发 Git 操作。

## 4. Subagent 启动契约

主 agent 启动 subagent 时，任务说明必须明确以下内容：

- 仓库的绝对路径和当前目标分支；
- 允许修改的文件，以及明确禁止修改的文件；
- 适用技术标准，本仓库主线默认是 ISO C++11；
- 必须阅读的项目规则和目标条目的上下文；
- 去重、编号、内容结构及标准行为与常见实现分离等要求；
- 必须执行的验证命令和验收条件；
- 是否允许暂存、提交和推送，以及提交信息和远端目标约束；
- 完成报告必须包含的提交哈希、文件列表、验证结果和远端状态；
- 当前共享工作区中的已知 writer、未提交改动或排队任务。

信息不足时，subagent 先只读调查并向主 agent 报告，不能自行扩大文件范围或发布权限。

## 5. 共享工作区与 Git 所有权

所有 agent 共享同一工作区和 Git 索引，必须把其他人的改动视为不可覆盖的在途工作。

- 开始和每次写入前运行 `git status --short --branch`，并检查活动 agent。
- 发现他人在目标文件中有未提交改动时，停止写入并排队；发现其他文件有改动时，只编辑自己明确拥有且不重叠的文件。
- 他人正在暂存或提交时不要同时操作 Git 索引。等待其完成后重新检查 `HEAD`、工作区和远端状态。
- 只显式暂存自己拥有的文件，例如 `git add -- AGENTS.md`；禁止使用 `git add .`、`git add -A` 或会夹带他人文件的命令。
- 提交前必须检查 `git diff --cached --name-only` 和完整的 `git diff --cached`。若索引包含非本任务文件，不得提交、重置或取消暂存他人的内容；应等待并报告冲突。
- 提交后使用 `git diff-tree --no-commit-id --name-only -r HEAD` 核对提交范围，并确认没有遗留自己的临时文件。
- 不得用重置、检出或覆盖操作清理不属于自己的改动。

## 6. 错题本 Agent 的独立验证

writer 不能仅依赖主 agent 的答案或其他 agent 的成功报告，必须独立验证最终文件：

1. 对新增或修改的 C++ 示例使用 `-std=c++11 -Wall -Wextra -pedantic` 做编译或 `-fsyntax-only` 检查；故意错误的示例要验证其失败原因符合描述。
2. 检查 Markdown 代码围栏成对。
3. 检查目录链接、`wNNN` 锚点、`WNNN` 标题和返回目录链接一一对应且无重复。
4. 运行 `pwsh -NoProfile -File .\tools\validate_learning_bank.ps1`。
5. 运行 `git diff --check`；提交前还要运行 `git diff --cached --check`。
6. 检查只修改获授权文件，内容中没有本机路径、令牌或个人信息。

任何检查失败都必须先修正并完整重跑；不能以“看起来正确”代替验证证据。

## 7. 安全提交与推送

- 只有启动契约明确授权时，subagent 才能提交或推送。
- 推送前先执行 `git fetch origin main`，再确认 `git merge-base --is-ancestor origin/main HEAD` 成功。
- 只有远端 `main` 是当前 `HEAD` 的祖先、推送会形成安全快进时，才允许 `git push origin HEAD:main`。
- 禁止 force push、`--force-with-lease`、重写远端历史或为绕过分叉而重置他人提交。
- 若远端已前进或发生分叉，停止推送，保留本地提交并向主 agent 报告双方哈希和建议的串行整合方式。
- 推送后再次 fetch，并核对 `origin/main` 与预期提交哈希一致。

## 8. 完成、冲突与失败报告

完成报告至少包含：

- 提交哈希和提交信息；
- 实际修改的文件；
- C++11 编译检查、Markdown 检查、题库验证和 `git diff --check` 的结果；
- 工作区是否干净，是否存在不属于本任务的改动；
- 是否推送、远端分支及远端最终哈希。

发生同文件冲突、索引被占用、验证失败、权限不足或远端分叉时，agent 必须停止危险步骤，说明：失败步骤、原始错误、受影响文件、本地与远端哈希、已完成的安全工作及下一步建议。不得静默覆盖、强推或谎报完成。

## 9. 何时不启动新的 Subagent

以下情况不需要启动新的 subagent：

- 非技术闲聊、只读查询、导航、状态说明或简单澄清；
- 用户明确要求不要写入错题本；
- 当前问题已由一个活动 wrongbook writer 接收，此时应追加或排队，而不是再启动 writer；
- 任务只需主 agent 当场回答，且没有任何持久化、独立验证或并行工作的需求；
- 目标文件所有权不清、共享索引被占用或缺少必要权限，此时先等待或报告阻塞。

“不启动新的 subagent”不等于延迟技术答案：主 agent 仍应先完成快问快答，再决定是否排队持久化。
